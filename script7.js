// arrays

var sam=["one","two","three"]

console.log('sam[1]: ', sam[1]);
console.log('sam[4]: ', sam[4]);

// To find the length of array
console.log('sam.length: ', sam.length);

// How to add value to an array?
sam[3]="four"
console.log('sam: ', sam);

sam[4] = "five"
sam[200] = "demojs"
console.log('sam: ', sam);

// it shows so many elements but lets check
console.log('sam.length: ', sam.length);

// even this doesnt show actual elements, it only shows largest index + 1


